# - When reporting a bug, ask the assistant to review code to understand the issue rather than just guessing. Confidence: 0.85
- When reporting a bug, ask the assistant to review code to understand the issue rather than just guessing. Confidence: 0.85
- Prefers explicit authorization gates between refactor work and behavior-change fixes (e.g. "ship the fix as a separate, explicitly-authorized commit"). Confidence: 0.9
- Appreciates root-cause analysis that traces a bug through multiple layers (frontend UI → backend route → background job → in-process cache → DB row) before proposing a fix. Confidence: 0.85
- Prefers fixes that close the loop on background jobs (post-install cache refresh, DB row update) rather than relying on the user to manually click a Refresh button. Confidence: 0.8
- Likes small, additive, behavior-preserving commits with a "What's NOT a bug" section that explicitly rules out adjacent code as the cause. Confidence: 0.8
- Prefers the assistant to read code and form a hypothesis from static analysis (no execution needed) when in plan mode, rather than running shell commands. Confidence: 0.75
- Prefers environment-agnostic fixes that do not assume a specific deployment topology (Docker, bare host, WSL, Apple Silicon, etc.) — solutions should not require OS-level PATH manipulation or container-specific configuration to work. Confidence: 0.85
- Prefers concise, abbreviated project/naming branding; considers multi-word descriptive names too long and seeks shorter domain or sub-domain alternatives to maintain brevity. Confidence: 0.75
- Communicates in and requests Arabic language for conversations. Confidence: 0.95
- Before authorizing wide-ranging changes (e.g., renames/rebranding), wants a precise inventory of every affected file/location found via repo search, and questions scope creep on peripheral assets such as logos. Confidence: 0.7
- Wants automated, interactive setup wizards that detect missing system prerequisites (node, python, redis, docker, build tools) and install them on the fly, with a user choice between native install and container (Docker) paths — including installing docker itself if missing and any non-requirements.txt system libraries the project needs (e.g. greenlet build deps). Confidence: 0.85
- Issues terse, action-oriented commands (e.g. "Open PR for the updates") and expects the assistant to execute the full pipeline (stage, commit, push, open PR) autonomously without step-by-step confirmation. Confidence: 0.85
- Wants PR-opening and other shared/external actions gated on credentials — the assistant should not prompt for tokens; when credentials are missing, produce portable handoff artifacts (patch file, PR body file, commit body) so the user can push from their own machine. Confidence: 0.9
- Wants project commits to exclude auto-generated/scaffolding directories (e.g. `.commandcode/taste/`) that are not part of the project's source — stage only files belonging to the actual change. Confidence: 0.85
- Appreciates thorough PR-body documentation files saved to the repo (e.g. `docs/pr-*.md`) that reviewers can read directly in the PR diff, with sections for Summary, Bug fix, New feature, Verification, How to test locally, and What's NOT a bug. Confidence: 0.8
- When referring to comparable prior-art UI/dashboards for a new feature, names specific products (e.g. CasaOS's Terminal tile, VS Code's integrated terminal, JupyterLab's terminal) — uses these as the UX benchmark the assistant should match. Confidence: 0.7
- When the user's stated scope is ambiguous, the assistant should offer a multi-option `ask_user_question` (typically 3–4 mutually exclusive scopes plus a "describe your own" path) before coding rather than guessing, because the user expects scope to be nailed down before implementation. Confidence: 0.7
- Likes documenting new WebSocket-backed endpoints with a per-product reverse-proxy section in `docs/` (nginx/Caddy/Traefik snippets with the exact `Upgrade` / `Connection: upgrade` / long `proxy_read_timeout` headers needed), because users will deploy behind at least one of these and silence on the proxy side means broken terminals in production. Confidence: 0.75
- Pushes back decisively when the assistant over-scopes or embellishes beyond the literal ask (e.g. "it's not like my idea — I need terminal only, can write the command, use like the official terminal"). Prefers the minimal, exact feature requested, with no adjacent panels/banners/onboarding flows bundled in; will redirect toward a stripped-down version rather than accept creep. Confidence: 0.9
- Treats "the official terminal" on the user's OS (macOS Terminal.app, Linux native shell) as the primary behavioral benchmark for any in-browser terminal feature — alongside CasaOS/VS Code/JupyterLab — expecting colors, cursor control, resize, vim/tmux/htop, and a clean prompt waiting for input without banners or preloaded commands. Confidence: 0.8
