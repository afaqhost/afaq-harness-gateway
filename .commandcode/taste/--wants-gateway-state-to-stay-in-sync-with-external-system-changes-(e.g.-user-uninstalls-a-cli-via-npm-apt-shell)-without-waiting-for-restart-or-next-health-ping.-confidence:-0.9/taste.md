- Wants gateway state to stay in sync with external system changes (e.g. user uninstalls a CLI via `npm uninstall -g`, `apt remove`, or deletes the binary from the shell) without waiting for restart or the next `/health` ping. Confidence: 0.9
- When a harness binary disappears, wants the DB `installed` flag, the in-memory MODEL_CACHE, and the Redis mirror all reconciled together in one atomic update (no half-states where DB says installed but cache is empty). Confidence: 0.85
- Prefers explicit `/uninstall` (or equivalent reconciliation) admin endpoints so the UI has a discoverable action for "forget this harness", rather than relying purely on background reconciliation — the user likes seeing a button that performs the sync visibly. Confidence: 0.85
- Wants runtime errors that result from stale state (e.g. `FileNotFoundError` from `create_subprocess_exec` after a binary was deleted) translated into clear, recoverable `RuntimeError` messages that mention the reconciliation endpoint, instead of leaking raw stack traces to the user. Confidence: 0.85
- Appreciates characterization tests being added when fixing bug classes that could regress (e.g. new `/uninstall` endpoint, reconcile path, error translation) — confirms the fix is locked in. Confidence: 0.8
- When the assistant makes a follow-up improvement to a previously committed fix, prefers amending the original commit (single coherent change) rather than stacking a second commit, provided the changes are tightly coupled to the same bug. Confidence: 0.8