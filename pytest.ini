[pytest]
asyncio_mode = auto
markers =
    unit: fast unit tests
    integration: integration with real DB
    e2e: end-to-end flows
    security: security and auth checks
    performance: performance and latency checks
    contract: API contract tests
    slow: slow tests
